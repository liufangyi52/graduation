# MeetingFlow AI - 智能体协作规范

## 项目结构
- `apps/web`：Vue 3 + Vite 前端。接口请求使用 `/api`，开发服务器将其代理至 3000 端口。
- `apps/api`：NestJS API 服务。全局前缀为 `/api`，默认端口为 3000。
- `apps/ai`：FastAPI 服务。默认端口为 8000。当前 `mock` 模式仅用于演示，不代表真实的大模型推理能力。
- `docker-compose.yml`：本地基础设施和容器启动配置；`.env.example` 是环境变量说明的唯一来源。

## 产品约束
- AI 输出均为待复核草稿。未经项目经理复核，不得创建正式任务，也不得将其表述为最终结果。
- 修改会议分析或任务抽取流程时，必须保留来源证据和引用字段。
- 不得将真实密钥、密码、令牌或个人会议数据写入源代码、示例、测试数据或文档。

## 工作规则
- 修改前先检查相关应用和现有行为；变更范围应与用户请求保持一致。
- 新增依赖、变更公共 API 契约、数据模型、Docker/环境配置或项目目录结构前，必须先征求用户确认。
- 不得丢弃、覆盖或回退用户已有的改动。
- Git 提交、推送、创建 PR 及破坏性命令必须获得用户明确授权。

## 验证要求
- 完成修改后，执行与变更范围匹配的最小验证。
- 涉及跨服务或面向发布的变更时，在项目根目录执行 `npm run build`。
- 修改 AI 服务时，在环境可用的前提下，至少验证 FastAPI 模块导入和 `/health` 接口契约。
- 交付时说明已执行的命令，以及因环境限制而无法完成的验证。

## 文档要求
- 用户可见工作流、启动命令、API 契约、环境变量或架构事实发生变化时，同步更新 README 或 `docs/`。
- 文档必须准确区分当前 mock/演示能力与计划中的持久化、RAG 或模型服务集成能力。
