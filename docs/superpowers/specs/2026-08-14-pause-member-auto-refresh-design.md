# 暂停成员页自动刷新设计

## 目标

项目详情的成员板块打开期间不因实时事件、重连或断线回退而自动重新加载整页。

## 设计

将现有仅针对分析页的自动刷新暂停判断扩展为分析页和成员页。实时连接仍继续建立，其他项目详情板块收到事件后仍可自动刷新；成员页只在初次进入项目详情、用户切换到其他支持同步的板块或手动的既有加载流程中更新。

## 验证

更新项目详情 UI 测试，断言刷新暂停计算同时覆盖 `analytics` 与 `members`，并运行定向 Vitest 测试和生产构建。
