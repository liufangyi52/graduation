# 移除项目动态标签设计

## 目标

从项目详情页移除没有独立内容的“动态”标签，避免其错误复用成员表。

## 设计

标签列表只保留概览、分析、任务、会议、风险和成员。成员表使用 `activeTab === 'members'` 的显式渲染条件；不匹配任何页面标签时不渲染成员表。后端已返回的项目动态数据、实时订阅和事件持久化不在本次变更范围内。

## 验证

更新项目详情 UI 测试，断言标签列表不再含 `activity`，且成员内容仅绑定 `members`。运行定向 Vitest 测试和生产构建。
