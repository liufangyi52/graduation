{
  "name": "meetingflow-ai",
  "private": true,
  "workspaces": ["apps/web", "apps/api"],
  "scripts": {
    "dev:web": "npm run dev -w @meetingflow/web",
    "dev:api": "npm run start:dev -w @meetingflow/api",
    "build": "npm run build -w @meetingflow/web && npm run build -w @meetingflow/api"
  }
}
